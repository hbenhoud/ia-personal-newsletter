# User profiles (contain personal data — injected from secrets in CI)
config/profile.md
config/profiles/

# Generated newsletter site
output/

# Embedding cache
.cache/

# Build binaries (repo-root only — must not match cmd/server, cmd/ingest, cmd/newsletter)
/newsletter
/server
/ingest

# Tailwind standalone CLI (downloaded locally; fetched in CI)
bin/

# Environment variables (API keys)
.env

